import React from 'react';

const About = () => {
  return (
    <div style={{width:'50%', margin:'5rem auto'}}>
      <h1>Description</h1>
      <p>In this project, i have used Bootstrap, React routing, React Chart, Dynamic routing and consitional routing</p>
      <p>It collects data from an API and shows the result in chart. It also shows the table of the list of data.By clicking the Details button it will all the data and whether the task is completed or Not on the basis of the task.</p>
    </div>
  );
};

export default About;